name: ekf_legged_robot_cpu_env
# Force ROS 2 to use localhost at startup for safety.
# This value can still be overridden after activating the environment.
variables:
  ROS_LOCALHOST_ONLY: "1"
channels:
- conda-forge
- robostack-staging
dependencies:
- pip
- python
- numpy
# Select the CPU build explicitly, without CUDA dependencies.
- pytorch=*=cpu*
- ros-humble-desktop
- ros-humble-rmw-cyclonedds-cpp
- ros-humble-rosidl-generator-dds-idl
- ros-humble-plotjuggler-ros
- compilers
- cmake
- pkg-config
- make
- ninja
- colcon-common-extensions
- catkin_tools
- rosdep
- tqdm
- gymnasium
- pip:
  - readchar
  - onnxruntime
  - warp-lang
  - mujoco-warp
